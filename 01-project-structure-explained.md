# 📐 المرحلة 1.1: فهم بنية المشروع (Project Structure)

---

## 🏗️ ما هي الـ Layered Architecture؟

ABP Framework بيستخدم معمارية الطبقات (Layered Architecture) المبنية على مبادئ **Domain Driven Design (DDD)**.

### الفكرة الأساسية:
بدل ما نكتب كل الكود في مشروع واحد، بنقسم المشروع لطبقات (Layers)، كل طبقة ليها دور محدد ومسؤولية واحدة.

---

## 📦 المشاريع الموجودة في الـ Solution

مشروعنا `Andro.Backend.Reference` مقسم لـ **9 مشاريع**، خلينا نفهم كل واحد:

### 🔵 1. Domain.Shared
**المسار:** `src/Andro.Backend.Reference.Domain.Shared`

**الدور:**
- أبسط طبقة في المشروع
- بتحتوي على الحاجات المشتركة بين كل الطبقات
- Constants, Enums, Static Values

**مثال على محتوياتها:**
- `Consts` - ثوابت عامة
- `Localization` - نصوص متعددة اللغات
- `Enums` - قوائم ثابتة (مثل OrderStatus: Pending, Completed, Cancelled)

**لماذا منفصلة؟**
لأنها بتستخدم في كل الطبقات، فلو حطيناها في Domain عادي، ممكن نعمل Circular Dependency

---

### 🔴 2. Domain
**المسار:** `src/Andro.Backend.Reference.Domain`

**الدور:** قلب المشروع 💓
- بتحتوي على **Business Logic** الأساسي
- بتعرف **Entities** (الكيانات اللي بتمثل جداول قاعدة البيانات)
- بتحتوي على **Domain Services** (خدمات معالجة الـ Business Logic المعقدة)
- بتعرف **Repository Interfaces** (كيف نتعامل مع البيانات)

**أمثلة:**
- `Product` Entity - منتج في نظام e-commerce
- `Order` Entity - طلب شراء
- `OrderManager` Domain Service - خدمة لإنشاء ومعالجة الطلبات

**قاعدة مهمة:**
الـ Domain Layer **مينفعش** يعتمد على أي طبقة تانية (ما عدا Domain.Shared)
ليه؟ علشان يكون مستقل وما يتأثرش بتغييرات في قاعدة البيانات أو الـ UI

---

### 🟢 3. Application.Contracts
**المسار:** `src/Andro.Backend.Reference.Application.Contracts`

**الدور:**
- بتحتوي على **Interfaces** لـ Application Services
- بتحتوي على **DTOs** (Data Transfer Objects) - الكائنات اللي بنستخدمها لنقل البيانات

**DTOs - إيه هي؟**
- كلاس بسيط فيه Properties فقط
- بنستخدمها علشان **ما نرجعش الـ Entity مباشرة** للـ Client
- بنتحكم في البيانات اللي هنرجعها بالظبط

**مثال:**
```csharp
// Entity (في الـ Domain) - كل البيانات
public class Product : Entity<Guid>
{
    public string Name { get; set; }
    public decimal Price { get; set; }
    public string InternalCode { get; set; } // سري
    public DateTime CreationTime { get; set; }
}

// DTO (في الـ Contracts) - بيانات محددة فقط
public class ProductDto
{
    public Guid Id { get; set; }
    public string Name { get; set; }
    public decimal Price { get; set; }
    // لاحظ: ما رجعناش InternalCode
}
```

---

### 🟡 4. Application
**المسار:** `src/Andro.Backend.Reference.Application`

**الدور:** طبقة تطبيق منطق الأعمال
- بتنفذ الـ **Application Services** (الخدمات اللي بيستخدمها الـ API)
- بتحول من Entity لـ DTO والعكس (Object Mapping)
- بتتعامل مع الـ Repositories
- بتطبق الـ **Use Cases** (حالات الاستخدام)

**مثال Use Case:**
عندك صفحة "قائمة المنتجات" - الـ Application Service بيعمل:
1. يجيب المنتجات من الـ Repository
2. يحولها من Entity لـ DTO
3. يرجعها للـ API

---

### 🟠 5. EntityFrameworkCore
**المسار:** `src/Andro.Backend.Reference.EntityFrameworkCore`

**الدور:** طبقة الـ Infrastructure - التعامل مع قاعدة البيانات
- بتحتوي على **DbContext** (بوابة التعامل مع قاعدة البيانات)
- بتعرف **جداول قاعدة البيانات** (DbSets)
- بتحتوي على **Configurations** للجداول (العلاقات، الـ Indexes، الـ Constraints)
- بتنفذ **Custom Repositories** لو محتاجين

**مثال:**
```csharp
public class BackendReferenceDbContext : AbpDbContext<BackendReferenceDbContext>
{
    public DbSet<Product> Products { get; set; }
    public DbSet<Order> Orders { get; set; }
}
```

---

### 🔵 6. HttpApi
**المسار:** `src/Andro.Backend.Reference.HttpApi`

**الدور:** طبقة الـ API
- بتحتوي على **Controllers** (نقاط الوصول للـ API)
- ABP بيولد Controllers تلقائياً من Application Services
- لو محتاج Custom Controller، بتكتبه هنا

**تلقائي vs يدوي:**
- ABP بيعمل auto-generate للـ Controllers من Application Services
- فمش محتاج تكتب Controllers بنفسك غالباً
- بس لو عايز تتحكم في Route أو HTTP Method، تقدر تعمل Custom Controller

---

### 🟣 7. HttpApi.Client
**المسار:** `src/Andro.Backend.Reference.HttpApi.Client`

**الدور:** كلاينت للتواصل مع الـ API
- بيستخدم في حالة **Microservices** أو لو عندك تطبيق تاني عايز يستهلك الـ API
- بيولد **Dynamic Proxies** تلقائياً
- بتستخدمه علشان تستدعي الـ API كأنها Method عادية بدل ما تكتب HTTP Requests يدوياً

---

### ⚪ 8. Web
**المسار:** `src/Andro.Backend.Reference.Web`

**الدور:** طبقة الـ UI (MVC/Razor Pages)
- **في حالتنا:** مش محتاجينه لأننا Backend فقط
- ممكن نحذفه أو نتجاهله
- لو عايزين نعمل Admin Panel مثلاً، ممكن نستخدمه

---

### 🟤 9. DbMigrator
**المسار:** `src/Andro.Backend.Reference.DbMigrator`

**الدور:** Console Application لإدارة قاعدة البيانات
- بينشئ قاعدة البيانات
- بيطبق الـ **Migrations** (التحديثات على البنية)
- بيضيف **Initial Data** (بيانات أولية مثل Admin User)

**متى نستخدمه؟**
- أول مرة تشغل المشروع
- كل ما نضيف Migration جديدة
- في الـ Production عند الـ Deployment

---

## 🔗 Dependencies بين المشاريع (من يعتمد على من)

```
┌─────────────────────────────────────────────────┐
│            Domain.Shared (القاعدة)              │
│         (Enums, Constants, Localization)        │
└─────────────────────────────────────────────────┘
                        ▲
                        │
        ┌───────────────┼───────────────┐
        │               │               │
┌───────┴────────┐     │     ┌─────────┴──────────┐
│    Domain      │     │     │ Application.Contracts│
│  (Entities,    │     │     │  (DTOs, Interfaces) │
│   Repositories)│     │     └──────────────────────┘
└────────────────┘     │               ▲
        ▲              │               │
        │              │     ┌─────────┴──────────┐
        │              │     │    Application      │
        │              │     │  (Services, Logic)  │
        │              │     └─────────────────────┘
        │              │               ▲
┌───────┴─────────┐    │               │
│ EntityFramework │    │     ┌─────────┴──────────┐
│     Core        │    │     │     HttpApi         │
│ (DbContext,     │────┘     │   (Controllers)     │
│  Repositories)  │          └─────────────────────┘
└─────────────────┘                    ▲
        ▲                              │
        │                    ┌─────────┴──────────┐
┌───────┴─────────┐          │   HttpApi.Client   │
│   DbMigrator    │          │    (Proxies)       │
│ (Migrations Run)│          └────────────────────┘
└─────────────────┘                    ▲
                                       │
                             ┌─────────┴──────────┐
                             │        Web         │
                             │    (UI - MVC)      │
                             └────────────────────┘
```

---

## ✅ الخلاصة

| المشروع | الدور | الاعتماد على |
|---------|-------|--------------|
| **Domain.Shared** | ثوابت، Enums | لا شيء |
| **Domain** | Business Logic، Entities | Domain.Shared |
| **Application.Contracts** | DTOs، Interfaces | Domain.Shared |
| **Application** | Application Services | Domain + Contracts |
| **EntityFrameworkCore** | قاعدة البيانات | Domain |
| **HttpApi** | API Controllers | Application |
| **HttpApi.Client** | API Client | Application.Contracts |
| **Web** | UI (اختياري) | HttpApi |
| **DbMigrator** | إدارة Database | EntityFrameworkCore |

---

## 🎯 الخطوة التالية

الآن بعد ما فهمنا البنية النظرية، خليني أفتح بعض الملفات الأساسية ونشوف الكود الفعلي!
